###### Std vector retrieval


+ relies on semantic similarity, missing attr like
    + prod categories
    + pub dates, access levels etc
+ fetches relevant looking, contentually incorrect chunks
+ retrieves outdated policies or restricted content;